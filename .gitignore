# Stale self-deploy output. The old deploy.sh could target this repo itself,
# leaving copies of the kit at the root. Those copies SHADOW the plugin's own
# skills when this repo is opened in a harness, so they must never be committed.
# deploy.sh is gone, so nothing regenerates these — delete any you still have.
/.claude/
/agent_docs/
/tasks/
/CLAUDE.md
/AGENTS.md

# Machine-local Compound Engineering config
/compound-engineering.local.md

# OS / editor noise
.DS_Store
*.swp
